using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Camera_raio : MonoBehaviour {
    

	// Use this for initialization
	void Start () {
        Camera.main.aspect = Screen.width / Screen.height;
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
